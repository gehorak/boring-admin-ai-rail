# Base Adoption Templates

The root `templates/` files remain the small documentation-first adoption
layer. `templates/operational/` is the strict front-matter set used only by
the Bootstrap-Complete public profile.
